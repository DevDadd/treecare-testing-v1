import 'package:flutter/material.dart';

class ResultDetailPage extends StatelessWidget {
  ResultDetailPage({super.key, required this.id});
  final int id;
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
